# Dataset Structure Tree Source Files

This folder is an audit bundle for understanding how the Dataset Structure Tree was generated. The browser visualization uses the embedded payload inside the outer `index.html`; these files are included for traceability and report review.

## Contents

- `build_interactive_dataset_visualization.py`: Generator script (copied).
- `main_column_metadata.csv`: Column-level metadata (copied).
- `main_field_metadata.csv`: Field-level metadata (copied).
- `main_file_inventory.csv`: Source file inventory (copied).
- `semantic_feature_groups.csv`: Semantic feature groups (copied).
- `semantic_feature_tree.json`: Semantic tree payload (copied).
- `semantic_group_coverage.csv`: Leaf/group coverage (copied).
- `semantic_node_coverage.csv`: Tree node coverage (copied).
- `tree_structure_review_candidates.csv`: Tree review candidates (copied).
- `tree_structure_audit_summary.csv`: Tree audit summary (copied).
- `requested_data_type_status.csv`: Requested data status (copied).
- `id_namespace_interpretation.csv`: ID namespace notes (copied).
- `dataset_alignment_bridge_summary.csv`: Bridge alignment summary (copied).
- `dataset_alignment_set_summary.csv`: Bridge-aware source counts (copied).
- `validation_report.json`: Validation report (copied).

Validation status: `PASS`; mapped raw columns: 22,530.
